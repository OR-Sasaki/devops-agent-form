# CONTEXT

このプロジェクトのユビキタス言語（用語集）。**実装の詳細は書かない** — それは `docs/plan/` に置く。
用語の意味が揺れたら、コードより先にこのファイルを直す。

---

## フォームアプリ (Form App)

利用者が触れる Web フォーム。本プロジェクトにおいて **これは目的ではなく手段** である。

DevOps Agent に調査させる対象を成立させるためだけに存在する。したがって「フォームとして優れているか」は評価軸ではなく、**「壊れたときに Agent が辿れる痕跡を残すか」** が評価軸になる。

## インシデント面 (Incident Surface)

フォームアプリが**壊れうる経路の総体**。ただし単に壊れるだけでは足りず、次の3つを満たしてはじめてインシデント面と呼ぶ。

1. **鳴る** — CloudWatch アラームが発火する
2. **残る** — ログ・メトリクスに原因まで辿れる痕跡が残る
3. **繋がる** — その痕跡がデプロイ履歴やコードに接続できる

インシデント面を持たないアプリは、本プロジェクトでは価値がゼロである。静的な S3 ホスティングのフォームが却下されたのはこの理由による。

## デモシナリオ (Demo Scenario)

「Agent にこれをやらせたい」という一本の筋書き。インシデント面を実際に発火させる手順と、Agent に期待する出力がセットになったもの。

本プロジェクトが成立したかどうかは、デモシナリオが**再現可能に**回せるかで判定する。

## Agent Space

DevOps Agent の境界を定める論理コンテナ。監視対象の AWS アカウント、外部連携（GitHub 等）、利用者アクセス、調査データのスコープをまとめて規定する。

作成リージョンに関わらず、紐づけたアカウントの**全リージョン**のリソースを監視できる。

## Agent Space ロール (Agent Space Role)

Agent Space に割り当てる IAM **ロール**。

> **DevOps Agent が何を見え、何をできるかを決めるのは、このロールだけである。**

人間が Terraform を回すときに使う IAM ユーザーとは**完全に無関係**。この2つは繰り返し混同されるため、必ず区別して呼ぶこと。

## デプロイ用アイデンティティ (Deployer Identity)

Terraform を実行し、インフラを変更する主体。「誰がインフラを壊せるか」を決める。

[Agent Space ロール](#agent-space-ロール-agent-space-role) とは別物であり、片方を絞ってももう片方は絞られない。

## 調査 (Investigation)

Agent がアラーム等を起点に、テレメトリとコードを突き合わせて根本原因を突き止める一連の動作。[agent-second](#agent-second) 課金の主対象。

## agent-second

DevOps Agent の課金単位。Agent が稼働した秒数で課金される。調査の所要時間がそのままコストになるため、**インシデント面を広く取りすぎると調査コストも比例して増える**。

## 管理アカウント (Management Account)

AWS Organizations の親アカウント。新しいデモアカウントを発行した元であり、請求もここに集約される。

**入口ではない。** [デモアカウント](#デモアカウント-demo-account)へは管理アカウントを経由せず直接サインインする。「アカウントを発行した元」と「作業時に通る場所」は別の話であり、この2つは混同されやすい。

## デモアカウント (Demo Account)

本プロジェクト専用に発行する、まっさらな新規メンバーアカウント。

「専用」であることが本質的に重要で、既存リソースがゼロだからこそ Agent が拾うものすべてが本プロジェクトのものになる。既存アカウントに相乗りしない理由はここにある。
